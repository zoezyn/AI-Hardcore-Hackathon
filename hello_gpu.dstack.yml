type: task
name: hello-gpu        # optional; random if omitted

image: winglian/axolotl-cloud:main-20240429-py3.11-cu121-2.2.1
commands:
  - echo "👋  from $(hostname)"
  - nvidia-smi          # prints GPU info

resources:
  gpu: A10G:1           # model:count   (or just 1 if you don’t care)
  memory: 16GB          # optional
